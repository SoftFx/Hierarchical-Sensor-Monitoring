All project instructions, architecture rules, and documentation live in [`AGENTS.md`](AGENTS.md).

AI-readable canonical docs: [`aicontext/`](aicontext/README.md).

## Agent skills

### Issue tracker

GitHub Issues in `SoftFx/Hierarchical-Sensor-Monitoring`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, each label string equal to its name; only `wontfix` exists in the repo so far. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root, ADRs under `docs/adr/`. See `docs/agents/domain.md`.
