@using HSMServer.Model.TreeViewModels
@using HSMServer.Core.Model.Authentication

@model List<TreeNodeStateViewModel>


<ul>
    @foreach (var product in Model)
        @await Html.PartialAsync("_TreeNode", product)
</ul>