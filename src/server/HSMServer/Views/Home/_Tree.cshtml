@model HSMServer.Model.TreeViewModels.TreeViewModel

<ul>
    @foreach (var (_, node) in Model.Nodes)
    {
        @if (node.Parent == null && node.IsAvailableForUser && node.VisibleSensorsCount != 0)
            @await Html.PartialAsync("_TreeNode", node)
    }
</ul>