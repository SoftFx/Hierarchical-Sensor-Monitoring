@using HSMServer.Extensions
@using HSMServer.Model.UserTreeShallowCopy

@model NodeShallowModel

@{
    var data = Model.Data;
}


<li id='@data.EncodedId' title='@data.Tooltip' data-jstree='@Model.ToJSTree()'>
    <span class="@(Model.IsIgnoredState ? "ignored-state-text" : "")">@Html.Raw(data.Name.GetShortNodeName()) @Model.SensorsCountString</span>

    @if (!data.IsEmpty)
    {
        @if (data.TelegramSettings.Chats.Count > 0)
        {
            @if (Model.GroupState.IsAllIgnored)
            {
                <span class="d-none">
                    <img class="tree-png-icon" id="groupsIgnoredNotifications" src="~/dist/snooze.png" alt="!"/>
                </span>
            }
            else if (!Model.GroupState.IsAnyEnabled)
            {
                <span class="d-none">
                    <i id="groupsDisabledNotifications" class="fa-solid fa-bell-slash"></i>
                </span>
            }
            @if (Model.IsGroupsEnable)
            {
                <span>
                    <img class="tree-png-icon" src="~/dist/users.png" alt="!" />
                </span>
            }
            @if (Model.IsAccountsEnable)
            {
                <span>
                    <img class="tree-png-icon" src="~/dist/user.png" alt="!" />
                </span>
            }
            
        }
        else
        {
            @* @if (Model.GroupState.IsAllIgnored) *@
            @* { *@
            @*     <span> *@
            @*         <img class="tree-png-icon" id="groupsIgnoredNotifications" src="~/dist/snooze.png" alt="!"/> *@
            @*     </span> *@
            @* } *@
            @* else if (!Model.GroupState.IsAnyEnabled) *@
            @* { *@
            @*     <span> *@
            @*         <i id="groupsDisabledNotifications tree-png-icon" class="fa-solid fa-bell-slash"></i> *@
            @*     </span> *@
            @* } *@
            
            @if (Model.IsGroupsEnable)
            {
                <span>
                    <img class="tree-png-icon" src="~/dist/users.png" alt="!" />
                </span>
            }
            @if (Model.IsAccountsEnable)
            {
                <span>
                    <img class="tree-png-icon" src="~/dist/user.png" alt="!" />
                </span>
            }
        }
        
        @if (Model.AccountState.IsAllIgnored)
        {
            <span class="d-none">
                <img class="tree-png-icon" id="accountsIgnoreNotifications" src="~/dist/user-crossed.png" alt="!"/>
            </span>
        }
        else if (Model.AccountState.IsAnyEnabled)
        {
            <span class="d-none">
                <img class="tree-png-icon" id="accountsNotifications" src="~/dist/user.png" alt="!"/>
            </span>
        }
    }

    @foreach (var child in Model.Nodes)
    {
        <ul>@await Html.PartialAsync("_TreeNode", child)</ul>
    }

    @if (Model.Sensors.Count > 0)
    {
        <ul>
            @foreach (var sensor in Model.Sensors)
            {
                sensor.IsParentHasGroupsEnabled = Model.IsGroupsEnable;
                sensor.IsParentHasAccountsEnabled = Model.IsAccountsEnable;
                @await Html.PartialAsync("_TreeSensor", sensor)
            }
        </ul>
    }
</li>